#include<stdio.h>#include <ctype.h>#include<string.h>#include<stdlib.h>#define MAX 80// int com_star(char[],char[]); //bool are_anagrams(const char *str, const char *str2){//void caesarCipher(char* message, int s1);int main(void){char message[MAX]; //maxium value which is 80char s2;int s1, ss;int i; int cypherValue;char cypher; int length;//fgets(plainText,sizeof(plainText),stdin);//fgets(message,sizeof(message),stdin);//scanf("%s",&s2);// printf("Enter shift amount (1-25):");//scanf("%d",&s1);printf("Enter message to be encrypted: "); // input message to be encryptedfor (i = 0; (s2 = getchar()) != '\n'; i++) {message[i] = s2;}while (i>80){ //checking if input is greater than 80 characters and loopsprintf("It's not valid\n");printf("Enter message to be encrypted: ");for (i = 0; (s2 = getchar()) != '\n'; i++) {message[i] = s2;}}ss= i;//promt the user to enter a number between 1-25printf("Enter shift amount (1-25):");scanf("%d",&s1);//loop over the message to generate the ciphered textprintf("The ciphered text is: ");//lower case and upper casesfor (i = 0; i < ss; i++) {s2 = message[i];if (s2 >='a' && s2 <= 'z')s2 = (( s2 - 'a') + s1) % 26 + 'a';if (s2 >= 'A' && s2 <= 'Z')s2 = (( s2 - 'A') + s1) % 26 + 'A';printf("%c", s2);//return 0;// while(message[i] != '\0' && strlen(message) - 1 > i){// cypherValue = ((int)message[i] - 97 + s1) % 26 + 97;// cypher =(char)(cypherValue);//printf("%c",cypher);//i++;//}}puts("");// printf("\n");return 0;}